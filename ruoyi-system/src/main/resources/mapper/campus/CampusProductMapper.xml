<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.campus.mapper.CampusProductMapper">

    <resultMap type="CampusProduct" id="CampusProductResult">
        <result property="productId"    column="product_id"    />
        <result property="userId"    column="user_id"    />
        <result property="title"    column="title"    />
        <result property="description"    column="description"    />
        <result property="category"    column="category"    />
        <result property="imageUrls"    column="image_urls"    />
        <result property="price"    column="price"    />
        <result property="contactInfo"    column="contact_info"    />
        <result property="viewCount"    column="view_count"    />
        <result property="status"    column="status"    />
        <result property="delFlag"    column="del_flag"    />
        <result property="createBy"    column="create_by"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateBy"    column="update_by"    />
        <result property="updateTime"    column="update_time"    />
        <result property="remark"    column="remark"    />
        <result property="nickName"   column="nick_name"   />
        <result property="avatar"   column="avatar"   />
        <result property="favorited"   column="favorited"   />
        <result property="userName"   column="user_name"   />
        <result property="college"   column="college"   />
        <result property="major"   column="major"   />
    </resultMap>

    <sql id="selectCampusProductVo">
        select p.product_id, p.user_id, p.title, p.description, p.category, p.image_urls, p.price, p.contact_info, p.view_count,
               p.status, p.del_flag, p.create_by, p.create_time, p.update_by, p.update_time, p.remark,
               u.nick_name, u.avatar, u.user_name, u.college, u.major,
               (case when f.favorite_id is not null then 1 else 0 end) as favorited
        from campus_product p
                 left join sys_user u on u.user_id = p.user_id
                 left join campus_product_favorite f on f.product_id = p.product_id and f.user_id = #{userId}
    </sql>

    <select id="selectCampusProductList" parameterType="CampusProduct" resultMap="CampusProductResult">
        <include refid="selectCampusProductVo"/>
        <where>
            p.del_flag = '0'
            <if test="category != null and category != ''"> and p.category = #{category}</if>
            <if test="title != null  and title != ''"> and p.title like concat('%', #{title}, '%')</if>
            <if test="status != null  and status != ''"> and p.status = #{status}</if>
            <if test="params.minPrice != null"> and p.price &gt;= #{params.minPrice}</if>
            <if test="params.maxPrice != null"> and p.price &lt;= #{params.maxPrice}</if>
        </where>
        order by p.create_time desc
    </select>

    <select id="selectCampusProductByProductId" parameterType="Long" resultMap="CampusProductResult">
        <include refid="selectCampusProductVo"/>
        where p.product_id = #{productId}
    </select>

    <insert id="insertCampusProduct" parameterType="CampusProduct" useGeneratedKeys="true" keyProperty="productId">
        insert into campus_product
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="userId != null">user_id,</if>
            <if test="title != null and title != ''">title,</if>
            <if test="description != null">description,</if>
            <if test="category != null and category != ''">category,</if>
            <if test="imageUrls != null">image_urls,</if>
            <if test="price != null">price,</if>
            <if test="contactInfo != null and contactInfo != ''">contact_info,</if>
            <if test="viewCount != null">view_count,</if>
            <if test="status != null and status != ''">status,</if>
            <if test="delFlag != null">del_flag,</if>
            <if test="createBy != null">create_by,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateBy != null">update_by,</if>
            <if test="updateTime != null">update_time,</if>
            <if test="remark != null">remark,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="userId != null">#{userId},</if>
            <if test="title != null and title != ''">#{title},</if>
            <if test="description != null">#{description},</if>
            <if test="category != null and category != ''">#{category},</if>
            <if test="imageUrls != null">#{imageUrls},</if>
            <if test="price != null">#{price},</if>
            <if test="contactInfo != null and contactInfo != ''">#{contactInfo},</if>
            <if test="viewCount != null">#{viewCount},</if>
            <if test="status != null and status != ''">#{status},</if>
            <if test="delFlag != null">#{delFlag},</if>
            <if test="createBy != null">#{createBy},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateBy != null">#{updateBy},</if>
            <if test="updateTime != null">#{updateTime},</if>
            <if test="remark != null">#{remark},</if>
        </trim>
    </insert>

    <update id="updateCampusProduct" parameterType="CampusProduct">
        update campus_product
        <trim prefix="SET" suffixOverrides=",">
            <if test="userId != null">user_id = #{userId},</if>
            <if test="title != null and title != ''">title = #{title},</if>
            <if test="description != null">description = #{description},</if>
            <if test="category != null and category != ''">category = #{category},</if>
            <if test="imageUrls != null">image_urls = #{imageUrls},</if>
            <if test="price != null">price = #{price},</if>
            <if test="contactInfo != null and contactInfo != ''">contact_info = #{contactInfo},</if>
            <if test="viewCount != null">view_count = #{viewCount},</if>
            <if test="status != null and status != ''">status = #{status},</if>
            <if test="delFlag != null">del_flag = #{delFlag},</if>
            <if test="createBy != null">create_by = #{createBy},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="remark != null">remark = #{remark},</if>
        </trim>
        where product_id = #{productId}
    </update>

    <delete id="deleteCampusProductByProductId" parameterType="Long">
        update campus_product set del_flag = '2' where product_id = #{productId}
    </delete>

    <delete id="deleteCampusProductByProductIds" parameterType="String">
        update campus_product set del_flag = '2' where product_id in
        <foreach item="productId" collection="array" open="(" separator="," close=")">
            #{productId}
        </foreach>
    </delete>

    <select id="selectCampusProductByProductIdForUpdate" resultMap="CampusProductResult">
        select
            p.product_id, p.user_id, p.title, p.description, p.image_urls, p.price, p.status, p.del_flag, p.create_by, p.create_time, p.update_by, p.update_time, p.remark, u.nick_name,
            u.user_name, u.college, u.major
        from campus_product p
                 left join sys_user u on u.user_id = p.user_id
        where p.product_id = #{productId}
            for update
    </select>

    <update id="updateCampusProductStatus">
        update campus_product
        set status = #{status}, update_time = sysdate()
        where product_id = #{productId} and status = '0' </update>

    <update id="incrementViewCount">
        update campus_product
        set view_count = IFNULL(view_count, 0) + 1
        where product_id = #{productId} and del_flag = '0'
    </update>

    <insert id="insertProductFavorite">
        insert into campus_product_favorite(product_id, user_id, create_time)
        values(#{productId}, #{userId}, now())
    </insert>

    <delete id="deleteProductFavorite">
        delete from campus_product_favorite
        where product_id = #{productId} and user_id = #{userId}
    </delete>

    <select id="checkProductFavorite" resultType="int">
        select count(*)
        from campus_product_favorite
        where product_id = #{productId} and user_id = #{userId}
    </select>

    <select id="selectMyProducts" parameterType="CampusProduct" resultMap="CampusProductResult">
        <include refid="selectCampusProductVo"/>
        <where>
            p.del_flag = '0' and p.user_id = #{userId}
            <if test="category != null and category != ''"> and p.category = #{category}</if>
            <if test="status != null and status != ''"> and p.status = #{status}</if>
        </where>
        order by p.create_time desc
    </select>

    <select id="selectMyFavoriteProducts" resultMap="CampusProductResult">
        select p.product_id, p.user_id, p.title, p.description, p.category, p.image_urls, p.price, p.contact_info, p.view_count,
               p.status, p.del_flag, p.create_by, p.create_time, p.update_by, p.update_time, p.remark,
               u.nick_name, u.avatar, u.user_name, u.college, u.major,
               1 as favorited
        from campus_product p
                 left join sys_user u on u.user_id = p.user_id
                 inner join campus_product_favorite f on f.product_id = p.product_id
        where f.user_id = #{userId} and p.del_flag = '0'
        order by f.create_time desc
    </select>

</mapper>